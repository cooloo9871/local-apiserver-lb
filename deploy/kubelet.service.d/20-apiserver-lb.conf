# kubelet drop-in: start the node-local apiserver load balancer before
# kubelet, and pull it in when kubelet starts.
#
# Install as /etc/systemd/system/kubelet.service.d/20-apiserver-lb.conf
# and run "systemctl daemon-reload".
[Unit]
After=apiserver-lb.service
Wants=apiserver-lb.service
